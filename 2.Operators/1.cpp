#include <bits/stdc++.h>
using namespace std;
int main()
{
    bool a = true;
    bool b = false;
    cout << (a & b) << endl; // and operation
    cout << (a | b) << endl; // or operation
    cout << (~a) << endl;    // not operartion
    cout << (a ^ b) << endl; // xor operation
}