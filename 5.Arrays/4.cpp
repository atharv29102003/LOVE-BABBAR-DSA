#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a[5];
    cout << "Enter the values in an array" << endl;
    for (int i = 0; i < 10; i++)
    {
        cin >> a[i];
    }
    cout << "Printing the values of an array" << endl;
    for (int i = 0; i < 10; i++)
    {
        cout << a[i] << endl;
    }
    return 0;
}